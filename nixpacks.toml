[phases.setup]
nixPkgs = [
  "php82",
  "php82Extensions.pdo_mysql",
  "composer",
  "nodejs_20",
  "npm-9_x"
]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install",
  "npm run build"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"